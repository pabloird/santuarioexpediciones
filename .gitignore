# macOS
.DS_Store

# Local tooling / browsers (may contain sessions/credentials)
.freebuff/

# Design tooling artifacts
.firecrawl/

# Logs / env
*.log
.env
.env.*

# s1lex design analysis (kept local, not in repo)
s1lex.com.md
s1lex.com.json

# Local AI tooling (session artifacts, not site files)
.commandcode/
.opencode/
.playwright-mcp/
